// import 'jest-extended';
// import { loggerFactory } from '../../main/config/ConfigLog4j';

describe("Name", () => {
    // const logger = loggerFactory.getLogger('test.Name');

    test("Create in Module II", () => {
        const name = new Name
        // expect().toBe(true);
    });

});
